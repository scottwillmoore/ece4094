{
	"private": true,
	"name": "ece4094",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "turbo run build",
		"clean": "turbo run clean",
		"develop": "turbo run develop --continue --parallel",
		"format": "prettier --write .",
		"test": "turbo run test"
	},
	"devDependencies": {
		"prettier": "^2.6.1",
		"prettier-plugin-sort-imports": "npm:@trivago/prettier-plugin-sort-imports@^3.2.0",
		"turbo": "^1.1.6"
	}
}
